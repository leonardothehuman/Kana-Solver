export const masterRoute: string = "/"; //Home page
export const mainDetailPage: string = "/extract/"; //Prefered detail page when we don't have what to show
export const masterDetailBreakpoint:number = 768; //Also on config.less
